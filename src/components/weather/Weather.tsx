import React from "react";
import styled from "styled-components";

type weatherProps = {
  history: History | null;
};

function Weather({ history }: weatherProps) {
  return <div></div>;
}

export default Weather;
